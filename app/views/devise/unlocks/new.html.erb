<div class="title"><h1>Resend Unlock Instructions</h1></div>
<br />

<%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| %>
    <%= devise_error_messages! %>

    <div>
      <%= f.label :email %><br />
        <%= f.email_field :email %>
    </div>

    <div>
        <%= f.submit "Resend Unlock Instructions" %>
    </div>

<% end %>

<%= render "links" %>